# akagi_supreme 評価レポート（最新）

## 第18回評価（2026-03-22）: トッププレイヤー思考の反映度 — A

---

### 1. 発見された問題

#### 1-1. _adjust_riichi が riichi_multiplier を Q値比較に適用していない（strategy_engine.py:405-413）
**問題**: `_adjust_riichi` で `riichi_multiplier < 0.8`（順位戦略がリーチを抑制）の場合、生のQ値を比較していた。Mortalがリーチを選んだ場合、riichi_q > discard_q が常に成り立つため、`q_diff = riichi_q - discard_q` はほぼ常に 0.05 以上となり、リーチ抑制が実質無効だった。例: multiplier=0.5 で riichi_q=0.20, discard_q=0.15 → 生比較では q_diff=0.05 でリーチされるが、調整後は 0.20*0.5=0.10 < 0.15+0.05=0.20 でダマテンが正しい。
**修正**: Q値比較前に `riichi_multiplier` を適用。正のQ値には乗算、負のQ値には除算（REVIEW.md基準6準拠）。
**深刻度**: 重大（オーラス1位リード時など、順位保護目的のダマテン判断が機能しないケースがあった）

#### 1-2. push_fold.py のイーシャンテン判定にデッドコード（push_fold.py:393-412）
**問題**: `evaluate_push_fold` のイーシャンテン処理で、`effective_threat >= 1.0` ブロック内の `effective_threat >= 1.8` 早期リターンにより `effective_threat < 1.8` が保証されている区間で、`effective_threat >= 2.0` や `>= 2.5` の判定が存在していた。これらは到達不可能なデッドコードであり、コードの可読性を損なっていた。
**修正**: 到達不可能な条件を削除し、`if my_turn >= 12:` に簡素化。到達可能なパス（`effective_threat >= 1.5` 等）は維持。
**深刻度**: 軽微（動作に影響なし。コード品質・保守性の問題）

---

### 2. 修正内容

- `strategy_engine.py`: `_adjust_riichi` のリーチ抑制分岐で、`riichi_multiplier` をQ値に適用してから比較するよう修正。正のQ値は乗算、負のQ値は除算で正しく処理。
- `push_fold.py`: イーシャンテン判定のデッドコード（`effective_threat >= 2.0/2.5` の到達不能ブランチ）を削除。外側条件を `if my_turn >= 12:` に簡素化。
- `tests/test_akagi_eval8.py`: 5件のテスト追加
  - `TestAdjustRiichiMultiplierApplied`: 3ケース（低multiplierでダマテン強制、圧倒的Q差でリーチ維持、負Q値の正しい処理）
  - `TestDeadCodeCleanup`: 2ケース（1位イーシャンテン終盤のフォールド、好形中打点の回し打ち）

---

### 3. テスト結果

- 既存テスト: 141件パス（1件は jsonschema 未インストールによる controller インポート失敗、akagi_supreme 無関係）
- 追加テスト: 5件
- 合計: 146件パス

---

### 4. 総合評価

| カテゴリ | 前回(第17回) | 今回(第18回) | 変化 |
|---------|-------------|-------------|------|
| 1. 攻守判断（押し引き） | A+ | A+ | → |
| 2. ベタオリ | A+ | A+ | → |
| 3. 回し打ち | A+ | A+ | → |
| 4. 副露判断 | A | A | → |
| 5. アガリ判断 | A+ | A+ | → |
| 6. リーチ/ダマテン判断 | A+ | S | ↑ riichi_multiplier適用修正で順位保護ダマテンが正しく機能 |
| 7. 順位戦略（オーラス） | A+ | A+ | → |
| 8. 順位戦略（南場・東場） | A | A | → |
| 9. 脅威度推定 | A+ | A+ | → |
| 10. 点数計算 | A+ | A+ | → |
| 11. 受入枚数 | A | A | → |
| 12. 3人麻雀 | A | A | → |
| 13. コード品質 | A+ | A+ | ↑ デッドコード除去 |

---

### 5. 詳細レビュー所見

#### 正しく実装されている主要項目
- **攻守判断**: PUSH/MAWASHI/FOLDの三値判断が明確。safety_weightによる二重カウントなし。リーチ宣言済みは無条件PUSH。`my_turn`（per-player巡目）を一貫使用。
- **ベタオリ**: FOLD時は現物→後付け現物→スジ→壁の優先順位。tile_base()による赤牌正規化一貫。孤立牌優先の正しいタイブレーク。共通安全牌（複数リーチ者に対して安全な牌）を最優先。
- **回し打ち**: 安全牌をSAFE/MODERATE/DANGEROUSの3段階に分類し、最安全カテゴリ内でQ値最高を選択。post_riichi_safe牌も安全として正しく認識。
- **副露判断**: FOLD時は鳴き拒否（オーラス4位例外）。門前維持価値の評価（テンパイ>イーシャンテン>リャンシャンテン）。七対子ルート検出で対子5組以上ならポン拒否。高打点時（8000点以上）は門前ペナルティ無視。
- **アガリ判断**: Mortalのhora見逃しを完全に尊重。無条件強制なし（REVIEW.md基準5に完全準拠）。
- **リーチ/ダマテン**: Q値負のリーチ乗算は除算で正しく処理。追っかけリーチはデフォルト強制だがラス回避薄差は例外化。終盤（14巡目以降）は1000点リスク回避でダマテン。1種1枚の極薄待ちはダマテン推奨。山残り分析（wait_tile_details）で精密判断。**riichi_multiplierがQ値比較に正しく適用（今回修正）**。
- **順位戦略（オーラス）**: 1位大差リード→FOLD、中差→MAWASHI、薄差→PUSH維持。2位は直撃条件も計算。3位はラス回避と上位逆転のバランス。4位は原則PUSH（降りても4位のまま）だがmin_push_value制限あり。
- **脅威度**: riichi_turnはper-player巡目で正しく格納。早リーチ+0.5、ツモ切り連続→手出しのダマテンシグナル、副露手打点推定（白ポンvs白ポン+中ポン+ホンイツを区別）、門前ホンイツ河検出、小三元検出すべて実装済み。
- **点数計算**: 三倍満親36000（正しい）。符計算は20/25/30/40/50に対応。`_calculate_points`と`estimate_hand_value`の整合性あり。
- **受入枚数**: 面子抽出で順子優先・刻子優先の両方を試行。3人麻雀で2m-8mを除外。テンパイ時の14枚完成手判定正常。
- **my_melds**: count_dora_in_hand, estimate_hand_value ともに副露牌を含めて計算（致命的バグ歴の再発なし）。
- **一発処理**: 各プレイヤーのippatsuフラグはそのプレイヤー自身の打牌時のみクリア。副露時は全員クリア（正しい一発消し）。

#### 許容可能な設計上の判断
- 東場の非親は原則Mortal委譲: 東場は順位変動が少なく介入リスクの方が大きい
- min_han_for_points のデフォルト30符: 戦略判断としては十分な精度
- noten_penalty_effect 固定-3000: 保守的推定（最悪ケース想定）で安全側
- ippatsu回避がshanten>=2でのみFOLD: トッププレイヤーもイーシャンテン以上なら一発巡目でも手牌状況で判断する

---

### 6. 総合ランク: A

**理由**: 致命的バグなし。今回発見されたriichi_multiplier未適用は重大な問題で、順位保護目的のダマテン判断（オーラス1位リードなど）が一部無効化されていたが修正済み。デッドコードも除去しコード品質向上。主要な13カテゴリすべてが正しく実装されており、リーチ/ダマテン判断はS評価に到達。S評価全体には、noten_penalty_effectの動的計算、実戦データによるA/Bテスト検証、および3人麻雀固有の戦略のさらなる最適化が必要。
